<%= form_for(@swf) do |f| %>
  <% if @swf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@swf.errors.count, "error") %> prohibited this swf from being saved:</h2>

      <ul>
      <% @swf.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :web %><br />
    <%= f.text_field :web %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
